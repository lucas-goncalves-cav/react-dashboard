# Host port used by docker compose
WEB_PORT=5173

# Base URL of the backend the app should consume.
# The demo runs against an in memory mock, so this is only needed once the
# services in src/services/api.ts are pointed at a real API.
VITE_API_URL=your_api_url_here
